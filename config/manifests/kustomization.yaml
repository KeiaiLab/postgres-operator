# OperatorHub bundle source — operator-sdk generate bundle 의 입력.
# CSV base + CRDs + RBAC + manager Deployment + webhook + samples.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- bases/postgres-operator.clusterserviceversion.yaml
- ../crd
- ../rbac
- ../manager
- ../samples
