# config/samples kustomization은 operator-sdk generate bundle 의 alm-examples
# 입력으로 쓰이며, OperatorHub UI 가 표시할 CR 예시 1 건씩을 owned CRD 별로 제공한다.
# e2e 테스트도 단일 entrypoint 로 본 kustomization 을 적용한다.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- postgres_v1alpha1_postgrescluster_dev.yaml
- postgres_v1alpha1_backupjob.yaml
- postgres_v1alpha1_scheduledbackup.yaml
- postgres_v1alpha1_pooler.yaml
- postgres_v1alpha1_postgresdatabase.yaml
- postgres_v1alpha1_postgresuser.yaml
- postgres_v1alpha1_imagecatalog.yaml
- postgres_v1alpha1_clusterimagecatalog.yaml
# - postgres_v1alpha1_postgrescluster_replica.yaml  # 외부 source 접근성 필요
# - postgres_v1alpha1_postgrescluster_prod.yaml  # production: storageClassName "fast-ssd" 필요
# - postgres_v1alpha1_pooler_podmonitor.yaml  # Prometheus Operator PodMonitor CRD 필요
